import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const StyledGreyBox = styled(Box)(() => ({
    backgroundColor: "#f5f5f5",
    borderRadius: "10px",
    padding: "10px",
    maxHeight: "max-content"
}));